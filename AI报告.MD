# 分析报告

## 1. 当前性能基准（核数据）

| 算子                   | 当前耗时 | PyTorch 耗时 | 倍数          | 已轮数 / 状态 |
| :--------------------- | :------- | :----------- | :------------ | :------------ |
| **conv2d**             | 0.84 ms  | 3.40 ms      | **2.4× ✅ 赢** | 1 轮就到位    |
| **gemm + bias + relu** | 0.273 ms | 0.477 ms     | **1.75×**     | 1 轮到位      |
| **gelu**               | 0.078 ms | 0.066 ms     | 0.84×         | 1 轮          |
| **layernorm**          | 1.32 ms  | 0.78 ms      | 0.59×         | ⚠️ 5 轮未动    |
| **softmax**            | 3.86 ms  | 1.13 ms      | 0.34×         | ⚠️ 5 轮未动    |

---

## 2. 优化方案全景（5 个"更狠"的方案，按预期回报/风险排序）

### 🟢 方案 A：Python 端 Cache Flush + 批大小折中（零风险，但已否决）
- **策略**：当前 `1024×512` 太大撑爆 L2 (1MB)。尝试裁成 `64×512` 块，分 16 块调用，期望每次调用 L2 全在 Cache 里。
- **预期计算**：`1.13 ms / 16 = 0.07 ms/块` → 总时间 `16 × 0.07 = 1.12 ms`。
- **最终结论**：**否决**。总 IO 读写量并未减少，理论上不会变快。

---

### 🟡 方案 B：挑战 PyTorch 的 Online Softmax 算法（高风险，高回报）
- **现状分析**：当前 Kernel 是 **3-pass**（分别计算 max / sum / normalize），内存 IO 为 `3 × 4MB = 12MB`。
- **优化策略**：改写为 **2-pass Online 算法**（单 pass 同时维持 running max 和 rescale sum）。
  - 伪代码逻辑：
    ```
    m = -inf, s = 0
    for i:
        if x_i > m: s = s * exp(m - x_i); m = x_i
        s += exp(x_i - m)
    for i:
        y_i = exp(x_i - m) / s
    ```
- **预期收益**：内存 IO 从 12MB 降至 `1×4MB + 1×4MB = 8MB`（节省 33% 带宽）。
- **预期耗时**：`3.86 ms → 约 2.6 ms`。
- **风险**：SIMD 实现逻辑较为复杂。

---

### 🟢 方案 C：精修测试 Kernel & 零拷贝转置校验（零风险，肉眼可检）
- **问题排查**：检查 `c_bridge` 中 softmax 的内存布局。
- **发现与验证**：
  - `c_rowmajor_gemm` 使用零拷贝转置技巧，将 Python 的 Row-major `(m, k)` 直接 reinterpret 为 Fortran 的 Col-major `(k, m)`。这正是 conv2d 取胜的关键。
  - Softmax 同理：Python `(n, dim)` Row-major 传入 Fortran 声明为 `a(dim, n)` Col-major。
  - 内层循环 `do i=1, d` 访问 `x(i, j)` 时，`i` 下标连续（stride=1），内存模式 **100% 正确**。
- **真正的慢因**：
  1. **3-pass 算法**（PyTorch 为 1-pass online）。
  2. **IO 带宽瓶颈**：总读写 12MB。
  3. **exp 实现**：`exp_avx512` 依赖多项式近似（非单条 FMA 指令），虽有 SIMD 流水线，但已接近极限。

---

### 🟢 方案 D：串行 Micro-Task 优化（零风险）
- **策略**：针对小批量（Batch）场景，增加条件判断 `if (n > 64)`，当数据量较小时强制串行执行，去掉多余的 OMP（OpenMP）多线程同步开销。
- **预期收益**：减少线程创建与同步延迟，提升小规模数据下的响应速度。

---

### 🟡 方案 E：手动 `_mm512_exp_pd` 内联替换（中风险）
- **策略**：在底层 Kernel 中，使用编译器内置函数（Intrinsics）直接内联 `_mm512_exp_pd`，替代当前可能存在的间接函数调用或虚函数跳转。
- **预期收益**：减少函数调用开销，保证指令级流水线效率，略为提升 exp 计算吞吐量。

---

## 3. 综合对比总结表

| #     | 方案                               | 风险等级         | 预期 Softmax 加速 | 预期 Layernorm 加速 |
| :---- | :--------------------------------- | :--------------- | :---------------- | :------------------ |
| **A** | Python 端 Cache Flush + 批大小折中 | 🟢 零（已否决）   | 无效（否决）      | 无效（否决）        |
| **B** | 2-pass Online Softmax 算法         | 🟡 中             | **1.3× ~ 1.5×**   | N/A                 |
| **C** | 精修 Kernel & 零拷贝校验           | 🟢 零（依赖现状） | 消除排查盲区      | 消除排查盲区        |
| **D** | 串行 Micro-Task（去 OMP 开销）     | 🟢 零             | **1.05× ~ 1.10×** | **1.05× ~ 1.10×**   |
| **E** | 手动 `_mm512_exp_pd` 内联替换      | 🟡 中             | **1.05× ~ 1.15×** | N/A                 |
